# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=client_portal
DB_USER=portal_user
DB_PASSWORD=change_me_to_a_strong_password

# Session
SESSION_SECRET=change_me_to_a_random_64_char_string

# Admin credentials (used for initial setup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me

# Server
PORT=3000
NODE_ENV=production

# Email notifications (optional - AWS SES)
# SES_FROM_EMAIL=noreply@yourdomain.com
# SES_TO_EMAIL=you@yourdomain.com
# AWS_REGION=us-east-1
